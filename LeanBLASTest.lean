/-
Test modules for LeanBLAS.

We intentionally keep tests under the `LeanBLASTest.*` namespace (instead of
`Test.*`) to avoid module-name collisions with other packages that ship
`Test.*` modules.
-/

